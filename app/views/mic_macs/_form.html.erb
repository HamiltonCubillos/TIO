<%= simple_form_for(@mic_mac) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :numvariables %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
